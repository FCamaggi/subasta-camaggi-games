services:
  - type: web
    name: subasta-camaggi-backend
    env: node
    region: oregon
    buildCommand: cd backend && npm install
    startCommand: cd backend && ./start-render.sh
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: ADMIN_PASSWORD
        value: cumpleaños2025
      - key: FRONTEND_URL
        sync: false
